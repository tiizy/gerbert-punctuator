import spacy


def split_raw_text(raw_text : str) -> list:
    """Splits raw text into the list of sentences.

    Args:
        raw_text (str):
    Returns:
        list_sentences (list):
    """
    pass

